services:
  - type: web
    name: tp1-alg2
    runtime: docker
    plan: free

    envVars:
      - key: PORT
        value: "10000"
      - key: NOMINATIM_DOMAIN
        value: "nominatim.openstreetmap.org"  # Nominatim externo

    autoDeployOnPush: true
    healthCheckPath: /
